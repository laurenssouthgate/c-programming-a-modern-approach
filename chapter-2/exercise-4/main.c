#include <stdio.h>

int main(void)
{
	int number;
	float float_one;
	int number_two;
	float float_two;
	int number_three;
	float float_three;

	printf("%d %f %d %f %d %f", number, float_one, number_two, float_two, number_three, float_three);

	return 0;
}
